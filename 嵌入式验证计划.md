# EdgeHome Harness 嵌入式验证计划

最后更新：2026-06-11

本文档是 EdgeHome Harness 的嵌入式验证专项计划，用于后续 `/goal` 或“追求目标模式”从 0 到 1 顺序执行。

它解决的问题不是“怎么继续写业务功能”，而是：

```text
如何在没有真实开发板之前，用 WSL2 + QEMU virt ARM64 + 2GB RAM 上限，
验证 EdgeHome Harness 是否具备端侧部署、低内存运行、ARM64 交叉编译、
trace/eval/replay、低内存降级和 MiniCPM5 边界实测能力。
```

核心结论先写清楚：

```text
QEMU virt ARM64 是嵌入式预验证环境，不是真实树莓派性能 benchmark。
E 盘解决磁盘空间问题，QEMU 的 -m 2048 才解决虚拟机 RAM 上限问题。
必跑主线先验证 Rust Harness + Mock 模型链路。
Ollama / MiniCPM5-1B 在 QEMU 中只做边界记录，不把 token/s 当成真实性能。
```

## 0. 文档地位

本文档不替代根目录的 `plan.md`。

二者关系：

```text
plan.md = EdgeHome Harness 产品和代码实现的唯一总规划。
嵌入式验证计划.md = ARM64 / 2GB RAM / QEMU / 部署验证专项规划。
```

当后续进入 `/goal` 模式执行嵌入式验证时，必须优先遵守本文档。

执行时不要重新发明路线，也不要临时把验证目标扩大成：

```text
自研推理引擎
替代 Ollama
完整米家网关
真实树莓派长期压测
真实 token/s 性能报告
```

本文档只追求一个明确目标：

```text
证明 EdgeHome Harness 可以被构建成 ARM64 Linux 二进制，
可以在 2GB RAM 约束的 Linux 环境里跑通 low_memory profile、
mock eval gate、内存压力策略、trace/replay/report，
并为后续真实开发板部署留下可复现脚本和报告。
```

## 1. 项目验证边界

### 1.1 这次要证明什么

必须证明：

```text
1. Ubuntu ARM64 虚拟机可启动。
2. 虚拟机架构为 aarch64。
3. 虚拟机 RAM 被锁定在约 2GB。
4. 所有 QEMU 镜像、构建缓存、报告产物放在 E 盘。
5. EdgeHome Harness 可以产出 ARM64 Linux release 二进制。
6. ARM64 二进制可以在虚拟机内运行。
7. low_memory profile 可以加载。
8. mock eval case 可以跑通。
9. eval --gate 可以通过。
10. config pressure 在不同 MemAvailable 水位下输出符合预期。
11. trace / replay / report 产物可保存。
12. 文档里能清楚说明 QEMU 验证结果和真实硬件验证结果的区别。
```

可选证明：

```text
1. QEMU 内可以安装 Ollama ARM64。
2. QEMU 内可以拉取 openbmb/minicpm5:latest。
3. MiniCPM5-1B 在 2GB RAM 虚拟机内能完成少量 structured output 请求。
4. 加载模型后的 MemAvailable 是否仍有 250MB-350MB 安全余量。
5. 如果发生 OOM、swap 抖动或超时，记录为 2GB + Ollama 路线边界，而不是硬扛。
```

### 1.2 这次不证明什么

本文档禁止得出以下结论：

```text
QEMU 完美复刻树莓派。
QEMU token/s 等于真实开发板 token/s。
QEMU 可以替代真实硬件长期稳定性测试。
2GB RAM 一定可以稳定长期运行 MiniCPM5 + Ollama + Harness。
项目已经完整支持米家 / MIoT / miIO / Matter / MQTT。
项目已经是可商用智能家居网关。
```

正确表述是：

```text
QEMU virt ARM64 2GB 用于 ARM64 Linux 部署预验证、2GB 内存水位验证、
low_memory profile 验证、OOM/fallback 验证、trace/eval/report 验证。

真实 token/s、真实散热、真实 I/O、真实局域网设备、真实长期稳定性，
必须在真实开发板上继续验证。
```

## 2. 总体路线

本计划分成 12 个阶段。

执行顺序固定：

```text
阶段 0：宿主机和仓库预检查
阶段 1：E 盘目录和产物规划
阶段 2：WSL2 安装 QEMU 工具链
阶段 3：下载 Ubuntu 24.04 ARM64 cloud image
阶段 4：生成 cloud-init seed
阶段 5：启动 QEMU virt ARM64 2GB
阶段 6：建立虚拟机内观测脚本
阶段 7：交叉编译 EdgeHome Harness ARM64 二进制
阶段 8：部署二进制和配置到虚拟机
阶段 9：跑通 Mock Harness low_memory 验证
阶段 10：跑通内存压力策略验证
阶段 11：可选 MiniCPM5 / Ollama 边界验证
阶段 12：生成嵌入式验证报告
```

不要跳阶段。

如果某阶段失败，先修该阶段，不要绕过。

## 3. 目录和磁盘规划

用户当前 C 盘空间紧张，因此 QEMU 相关内容默认放到 E 盘。

Windows 路径：

```powershell
E:\edgehome-qemu
```

WSL 路径：

```bash
/mnt/e/edgehome-qemu
```

目录规划：

```text
/mnt/e/edgehome-qemu/
  images/                  Ubuntu ARM64 qcow2、seed.img、UEFI vars
  cargo-target/            Rust 交叉编译 target 目录
  artifacts/               内存快照、eval 输出、trace 导出、最终报告素材
  models/                  可选模型缓存说明，不强行迁移 Ollama 默认目录
  scripts/                 后续沉淀的 QEMU 启动和采集脚本
  repo-copy/               可选，ASCII 路径构建副本
```

重要区分：

```text
E 盘 = 磁盘空间。
QEMU -m 2048 = 虚拟机总 RAM 上限。
```

把文件放 E 盘不会让虚拟机有更多内存。

2GB RAM 验证只由 QEMU 启动参数控制：

```bash
-m 2048
```

## 4. 阶段 0：宿主机和仓库预检查

### 4.1 检查仓库状态

在 PowerShell 中执行：

```powershell
cd C:\Users\xiaoy\Desktop\后端\EdgeHome-Harness
git status --short --branch
```

验收：

```text
明确当前分支。
明确本地是否领先远端。
明确是否存在未提交改动。
不要覆盖用户已有改动。
```

当前已知注意点：

```text
docs/small-model-harness-blog.md 可能存在用户未提交改动。
嵌入式验证相关提交不要误把这份博客改动一起提交，除非用户明确要求。
```

### 4.2 检查 WSL2

在 PowerShell 中执行：

```powershell
wsl -l -v
```

验收：

```text
存在 Ubuntu 发行版。
VERSION 为 2。
```

如果没有 WSL2，先安装 Ubuntu WSL2，不在本文档内展开。

### 4.3 检查 E 盘空间

在 PowerShell 中执行：

```powershell
Get-PSDrive E
```

在 WSL 中执行：

```bash
df -h /mnt/e
```

最低要求：

```text
可用空间 >= 20GB
推荐可用空间 >= 40GB
```

原因：

```text
Ubuntu ARM64 cloud image 扩容后建议 20GB。
Rust cargo target 可能占用数 GB。
后续 Ollama / 模型缓存 / trace / 报告还会继续占用空间。
```

### 4.4 检查 Windows 本机 Cargo 构建风险

当前项目曾经出现过一个环境问题：

```text
中文路径 + Windows GNU Rust toolchain 可能导致 proc-macro / linker 临时文件异常。
```

因此嵌入式验证主线不要依赖根目录中文路径下的 Windows 构建结果。

推荐后续构建路线：

```text
优先在 WSL2 内使用 ASCII 路径构建和交叉编译。
Cargo target 放到 /mnt/e/edgehome-qemu/cargo-target。
```

如果必须在 Windows PowerShell 构建，则至少设置：

```powershell
$env:CARGO_TARGET_DIR="E:\edgehome-qemu\cargo-target"
```

但嵌入式验证阶段仍推荐 WSL2。

## 5. 阶段 1：创建 E 盘工作目录

在 WSL2 Ubuntu 中执行：

```bash
mkdir -p /mnt/e/edgehome-qemu/images
mkdir -p /mnt/e/edgehome-qemu/cargo-target
mkdir -p /mnt/e/edgehome-qemu/artifacts
mkdir -p /mnt/e/edgehome-qemu/scripts
mkdir -p /mnt/e/edgehome-qemu/repo-copy
cd /mnt/e/edgehome-qemu
```

验收：

```bash
pwd
find /mnt/e/edgehome-qemu -maxdepth 1 -type d -print
```

必须看到：

```text
/mnt/e/edgehome-qemu
/mnt/e/edgehome-qemu/images
/mnt/e/edgehome-qemu/cargo-target
/mnt/e/edgehome-qemu/artifacts
/mnt/e/edgehome-qemu/scripts
/mnt/e/edgehome-qemu/repo-copy
```

提交要求：

```text
这个阶段不需要提交仓库。
只是在 E 盘创建本地验证目录。
```

## 6. 阶段 2：WSL2 安装 QEMU 工具链

在 WSL2 Ubuntu 中执行：

```bash
sudo apt update
sudo apt install -y \
  qemu-system-arm \
  qemu-utils \
  qemu-efi-aarch64 \
  cloud-image-utils \
  wget \
  curl \
  xz-utils \
  openssh-client \
  ca-certificates
```

验收：

```bash
qemu-system-aarch64 --version
qemu-img --version
cloud-localds --version || true
```

判断标准：

```text
qemu-system-aarch64 能输出版本。
qemu-img 能输出版本。
cloud-localds 能运行或至少存在。
```

失败处理：

```text
如果 qemu-system-aarch64 不存在，检查 qemu-system-arm 是否安装成功。
如果 cloud-localds 不存在，检查 cloud-image-utils 是否安装成功。
如果 apt 下载失败，先修 WSL 网络，不要切换到 Windows QEMU 路线。
```

## 7. 阶段 3：下载 Ubuntu 24.04 ARM64 cloud image

进入镜像目录：

```bash
cd /mnt/e/edgehome-qemu/images
```

下载 Ubuntu 24.04 ARM64 cloud image：

```bash
wget https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-arm64.img \
  -O ubuntu-24.04-arm64.qcow2
```

扩容镜像到 20GB：

```bash
qemu-img resize ubuntu-24.04-arm64.qcow2 20G
```

检查镜像：

```bash
ls -lh ubuntu-24.04-arm64.qcow2
qemu-img info ubuntu-24.04-arm64.qcow2
```

验收：

```text
镜像文件位于 /mnt/e/edgehome-qemu/images。
qemu-img info 显示 virtual size 约 20G。
```

失败处理：

```text
如果下载中断，删除不完整 qcow2 后重下。
如果 E 盘空间不足，不要放回 C 盘，先清理 E 盘或缩小后续计划。
```

## 8. 阶段 4：生成 cloud-init seed

进入镜像目录：

```bash
cd /mnt/e/edgehome-qemu/images
```

创建 `user-data`：

```bash
cat > user-data <<'EOF'
#cloud-config
hostname: edgehome-qemu
manage_etc_hosts: true

users:
  - name: edge
    groups: sudo
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false
    plain_text_passwd: edgehome

ssh_pwauth: true
disable_root: false

package_update: true
packages:
  - curl
  - git
  - build-essential
  - pkg-config
  - sqlite3
  - htop
  - jq
  - ca-certificates

runcmd:
  - [ sh, -c, "echo 'edge ALL=(ALL) NOPASSWD:ALL' >/etc/sudoers.d/edge" ]
EOF
```

创建 `meta-data`：

```bash
cat > meta-data <<'EOF'
instance-id: edgehome-qemu
local-hostname: edgehome-qemu
EOF
```

生成 seed 镜像：

```bash
cloud-localds seed.img user-data meta-data
```

复制 UEFI 固件：

```bash
cp /usr/share/AAVMF/AAVMF_CODE.fd ./AAVMF_CODE.fd
cp /usr/share/AAVMF/AAVMF_VARS.fd ./AAVMF_VARS.fd
```

如果路径不存在，执行：

```bash
find /usr/share -name 'AAVMF*.fd'
```

再按实际路径复制。

验收：

```bash
ls -lh seed.img AAVMF_CODE.fd AAVMF_VARS.fd
```

必须存在：

```text
seed.img
AAVMF_CODE.fd
AAVMF_VARS.fd
```

安全说明：

```text
user-data 中的 edgehome 密码只用于本地 QEMU 验证。
不要把这个账号密码用于公网服务器。
```

## 9. 阶段 5：启动 QEMU virt ARM64 2GB

进入镜像目录：

```bash
cd /mnt/e/edgehome-qemu/images
```

启动命令：

```bash
qemu-system-aarch64 \
  -machine virt \
  -cpu cortex-a72 \
  -smp 2 \
  -m 2048 \
  -nographic \
  -drive if=pflash,format=raw,readonly=on,file=AAVMF_CODE.fd \
  -drive if=pflash,format=raw,file=AAVMF_VARS.fd \
  -drive if=virtio,format=qcow2,file=ubuntu-24.04-arm64.qcow2 \
  -drive if=virtio,format=raw,file=seed.img \
  -netdev user,id=net0,hostfwd=tcp::2222-:22 \
  -device virtio-net-pci,netdev=net0
```

关键参数说明：

```text
-machine virt      使用 QEMU 通用 ARM64 virt 机器，稳定、适合工程验证。
-cpu cortex-a72    模拟 ARM Cortex-A72 级别 CPU。
-smp 2             两个虚拟 CPU，贴近低端边缘设备。
-m 2048            虚拟机总内存锁死 2GB，这是本计划最关键参数。
-nographic         无图形界面，贴近 server / embedded 部署。
hostfwd 2222       宿主机通过 localhost:2222 SSH 进虚拟机。
```

首次启动可能需要等待 cloud-init 完成。

本地登录：

```text
user: edge
password: edgehome
```

宿主机另开一个 WSL 终端，尝试 SSH：

```bash
ssh edge@localhost -p 2222
```

进入虚拟机后执行：

```bash
uname -m
free -h
cat /proc/meminfo | egrep 'MemTotal|MemAvailable|SwapTotal|SwapFree'
```

验收：

```text
uname -m 输出 aarch64。
MemTotal 接近 2GB。
SSH 可以通过 localhost:2222 登录。
```

失败处理：

```text
如果不能 SSH，先等 cloud-init 完成，再检查端口 2222 是否被占用。
如果 MemTotal 不是约 2GB，检查 QEMU 是否确实使用 -m 2048。
如果虚拟机不能启动，优先检查 AAVMF 固件、qcow2 路径和 seed.img。
```

退出 QEMU：

```text
在 QEMU 控制台中可使用 Ctrl+A 然后 X 退出。
也可以在虚拟机内执行 sudo poweroff。
```

## 10. 阶段 6：虚拟机内基础观测脚本

进入虚拟机后创建工作目录：

```bash
mkdir -p /home/edge/edgehome/artifacts
mkdir -p /home/edge/edgehome/scripts
```

创建内存采集脚本：

```bash
cat > /home/edge/edgehome/scripts/collect-memory.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

echo "===== timestamp ====="
date -Is
echo
echo "===== uname ====="
uname -a
echo
echo "===== free ====="
free -h
echo
echo "===== meminfo ====="
cat /proc/meminfo | egrep 'MemTotal|MemFree|MemAvailable|Buffers|Cached|SwapTotal|SwapFree'
echo
echo "===== top rss ====="
ps -eo pid,comm,rss,vsz,%mem --sort=-rss | head -20
EOF

chmod +x /home/edge/edgehome/scripts/collect-memory.sh
```

采集 baseline：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/baseline-before-edgehome.txt
```

验收：

```bash
ls -lh /home/edge/edgehome/artifacts/baseline-before-edgehome.txt
grep -E 'MemTotal|MemAvailable' /home/edge/edgehome/artifacts/baseline-before-edgehome.txt
```

必须记录：

```text
系统启动后的 baseline MemAvailable。
top RSS 进程列表。
SwapTotal / SwapFree。
```

后续每个关键节点都要采集：

```text
baseline-before-edgehome.txt
after-edgehome-binary-copy.txt
after-edgehome-config-show.txt
after-eval-gate.txt
after-pressure-policy.txt
after-ollama-install.txt            可选
after-minicpm5-load.txt             可选
after-minicpm5-structured-call.txt  可选
```

## 11. 阶段 7：交叉编译 EdgeHome Harness ARM64 二进制

### 11.1 推荐路线

推荐在 WSL2 宿主环境交叉编译，不推荐在 QEMU 虚拟机内直接编译。

原因：

```text
QEMU 内编译慢。
QEMU 内只有 2GB RAM，编译会干扰运行验证。
宿主机交叉编译更符合真实嵌入式项目流程。
```

### 11.2 准备 ASCII 路径仓库副本

由于当前仓库路径包含中文目录，WSL / Windows / Rust 工具链组合可能出现路径兼容问题。

推荐在 E 盘建立 ASCII 路径副本：

```bash
cd /mnt/e/edgehome-qemu/repo-copy
git clone /mnt/c/Users/xiaoy/Desktop/后端/EdgeHome-Harness EdgeHome-Harness
cd EdgeHome-Harness
```

如果 WSL 无法正确访问中文路径，可以改用 GitHub 地址：

```bash
cd /mnt/e/edgehome-qemu/repo-copy
git clone git@github.com:yushui2022/EdgeHome-Harness.git EdgeHome-Harness
cd EdgeHome-Harness
```

如果本地有未 push 提交，优先使用本地 clone，确保验证的是当前最新工作。

验收：

```bash
git status --short --branch
ls
```

必须看到：

```text
Cargo.toml
crates/
configs/
cases/
```

### 11.3 安装 Rust 和交叉编译工具链

在 WSL2 宿主环境执行：

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source "$HOME/.cargo/env"
rustup target add aarch64-unknown-linux-gnu
sudo apt update
sudo apt install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu pkg-config
```

设置构建输出到 E 盘：

```bash
export CARGO_TARGET_DIR=/mnt/e/edgehome-qemu/cargo-target
export CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc
```

建议把本次终端里的环境变量写入临时记录：

```bash
env | grep -E 'CARGO_TARGET_DIR|AARCH64' \
  | tee /mnt/e/edgehome-qemu/artifacts/build-env.txt
```

### 11.4 先做宿主机 check

在 ASCII 路径仓库执行：

```bash
cargo check --workspace
```

验收：

```text
cargo check --workspace 通过。
```

如果失败：

```text
先修项目代码或依赖，不进入 QEMU 验证。
不要把编译失败误判成 QEMU 问题。
```

### 11.5 交叉编译 release 二进制

执行：

```bash
cargo build --release --target aarch64-unknown-linux-gnu -p edgehome-cli
```

生成文件：

```bash
ls -lh /mnt/e/edgehome-qemu/cargo-target/aarch64-unknown-linux-gnu/release/edgehome
file /mnt/e/edgehome-qemu/cargo-target/aarch64-unknown-linux-gnu/release/edgehome
```

验收：

```text
file 输出包含 ARM aarch64 或 ARM64。
二进制名为 edgehome。
```

失败处理：

```text
如果 linker 找不到，确认 CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER。
如果 sqlite/rusqlite 链接失败，先确认 bundled sqlite 是否正常编译。
如果 C 盘空间异常增长，确认 CARGO_TARGET_DIR 已指向 /mnt/e/edgehome-qemu/cargo-target。
```

提交要求：

```text
如果只是本地构建，不需要提交。
如果为了支持交叉编译修改了项目配置，例如 .cargo/config.toml，必须单独提交。
```

## 12. 阶段 8：部署二进制和配置到虚拟机

确保 QEMU 正在运行，宿主机可以 SSH：

```bash
ssh edge@localhost -p 2222 'uname -m && free -h'
```

复制二进制：

```bash
scp -P 2222 \
  /mnt/e/edgehome-qemu/cargo-target/aarch64-unknown-linux-gnu/release/edgehome \
  edge@localhost:/home/edge/edgehome/edgehome
```

复制配置和用例：

```bash
cd /mnt/e/edgehome-qemu/repo-copy/EdgeHome-Harness
scp -P 2222 -r configs cases edge@localhost:/home/edge/edgehome/
```

进入虚拟机：

```bash
ssh edge@localhost -p 2222
cd /home/edge/edgehome
chmod +x ./edgehome
./edgehome --help
```

采集部署后内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-edgehome-binary-copy.txt
```

验收：

```text
./edgehome --help 能输出 CLI 帮助。
configs/low_memory.yaml 存在。
cases/zh-home.yaml 存在。
after-edgehome-binary-copy.txt 已生成。
```

## 13. 阶段 9：Mock Harness low_memory 验证

这一阶段是必跑主线。

目的：

```text
先验证 EdgeHome Harness 自身在 ARM64 + 2GB 环境下可运行。
不依赖 Ollama。
不依赖真实设备。
不依赖真实 Home Assistant。
```

### 13.1 查看 low_memory profile

在虚拟机内执行：

```bash
cd /home/edge/edgehome
./edgehome --profile low_memory --config-dir configs config show \
  | tee artifacts/config-show-low-memory.json
```

验收：

```bash
jq '.name, .model_name, .num_ctx, .num_predict, .retry_count, .memory_enabled, .executor_backend' \
  artifacts/config-show-low-memory.json
```

必须符合：

```text
name = low_memory
model_name = openbmb/minicpm5:1b
num_ctx <= 1024
num_predict <= 128
retry_count <= 1
memory_enabled = true
executor_backend = mock
```

采集内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-edgehome-config-show.txt
```

### 13.2 单条 mock parse 验证

执行：

```bash
./edgehome --profile low_memory --config-dir configs --db-path artifacts/qemu-parse.sqlite \
  parse --mock "把卧室空调打开" \
  | tee artifacts/mock-parse-open-bedroom-ac.json
```

最低验收：

```text
输出为 JSON。
包含 normalized_command 或等价规范化结果。
不需要访问 Ollama。
不需要访问真实设备。
```

检查：

```bash
jq '.' artifacts/mock-parse-open-bedroom-ac.json >/dev/null
```

### 13.3 单条 mock dry-run 验证

执行：

```bash
./edgehome --profile low_memory --config-dir configs --db-path artifacts/qemu-dry-run.sqlite \
  dry-run --mock "把卧室空调打开" \
  | tee artifacts/mock-dry-run-open-bedroom-ac.json
```

最低验收：

```text
输出包含 trace_id。
输出包含 policy_decision。
输出包含 dry_run_plan 或拒绝原因。
真实 executor 不执行。
```

检查：

```bash
jq '.trace_id, .policy_decision, .dry_run_plan' artifacts/mock-dry-run-open-bedroom-ac.json
```

### 13.4 跑完整 eval

执行：

```bash
./edgehome --profile low_memory --config-dir configs --db-path artifacts/qemu-eval.sqlite \
  eval cases/zh-home.yaml \
  | tee artifacts/eval-low-memory.json
```

验收：

```bash
jq '.report' artifacts/eval-low-memory.json
```

必须至少看到以下指标：

```text
intent_accuracy
slot_accuracy
policy_accuracy
dry_run_accuracy
trace_coverage
schema_valid_rate
memory_resolution_accuracy
fallback_rate
dead_loop_rate
retry_rate
low_memory_degrade_count
```

### 13.5 跑 release gate

执行：

```bash
./edgehome --profile low_memory --config-dir configs --db-path artifacts/qemu-gate.sqlite \
  eval cases/zh-home.yaml --gate \
  | tee artifacts/eval-low-memory-gate.json
```

验收：

```bash
jq '.gate.passed, .gate.checks' artifacts/eval-low-memory-gate.json
```

必须满足：

```text
gate.passed = true
pass_rate = 1.0
schema_valid_rate = 1.0
dead_loop_rate = 0.0
trace_coverage = 1.0
intent_accuracy >= 0.95
slot_accuracy >= 0.90
retry_rate <= 0.30
```

采集内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-eval-gate.txt
```

失败处理：

```text
如果 eval --gate 不通过，必须先修 Harness、case 或 profile。
不要进入 Ollama/MiniCPM5 可选阶段。
不要通过降低 gate 标准掩盖失败。
```

## 14. 阶段 10：内存压力策略验证

这一阶段验证 `ResourcePressurePolicy` 是否符合 2GB RAM 设计。

执行：

```bash
cd /home/edge/edgehome

./edgehome --profile low_memory --config-dir configs \
  config pressure --free-memory-mb 1024 \
  | tee artifacts/pressure-1024.json

./edgehome --profile low_memory --config-dir configs \
  config pressure --free-memory-mb 400 \
  | tee artifacts/pressure-400.json

./edgehome --profile low_memory --config-dir configs \
  config pressure --free-memory-mb 128 \
  | tee artifacts/pressure-128.json
```

检查：

```bash
jq '.' artifacts/pressure-1024.json
jq '.' artifacts/pressure-400.json
jq '.' artifacts/pressure-128.json
```

预期方向：

```text
free_memory_mb = 1024
  系统应保持正常或接近正常配置。
  memory_enabled 应保持 true。
  num_ctx / num_predict 不应超过 low_memory 上限。

free_memory_mb = 400
  系统应进入更保守策略。
  输出长度、上下文或注入策略应收缩。
  memory_enabled 通常仍可保持 true，但长期记忆注入应谨慎。

free_memory_mb = 128
  系统应进入 critical / rule-only / 强降级方向。
  memory_enabled 应可能关闭或显著收缩。
  num_ctx / num_predict 应进一步降低。
```

注意：

```text
具体字段名以当前 edgehome-cli 输出为准。
本文档要求验证策略方向，而不是强写死不存在的字段。
```

采集内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-pressure-policy.txt
```

验收：

```text
三个 pressure 输出均为合法 JSON。
1024 / 400 / 128 三个水位能体现从正常到降级的策略变化。
critical 水位下不能扩大上下文、输出长度或记忆注入。
```

失败处理：

```text
如果 pressure 输出不随 free_memory_mb 变化，说明低内存策略没有验证价值。
必须回到代码层修 ResourcePressurePolicy 或 profile 映射。
```

## 15. 阶段 11：可选 MiniCPM5 / Ollama 边界验证

这一阶段是可选阶段。

只有阶段 9 和阶段 10 全部通过后才允许执行。

### 15.1 为什么可选

原因：

```text
QEMU TCG 模拟 ARM64 CPU 很慢。
Ollama 在虚拟化 ARM64 环境中的运行表现不等于真实树莓派。
MiniCPM5 加载后的内存占用、page cache、KV cache 和请求波动才是主要观察目标。
token/s 不能作为真实硬件性能结论。
```

### 15.2 安装 Ollama

在虚拟机内执行：

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

检查：

```bash
ollama --version
systemctl status ollama --no-pager || true
```

采集内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-ollama-install.txt
```

失败处理：

```text
如果安装脚本不支持当前 QEMU 环境，记录失败原因，停止 MiniCPM5 阶段。
不要为了安装 Ollama 大规模改变项目目标。
```

### 15.3 拉取模型

用户当前模型：

```text
openbmb/minicpm5:latest
模型包大小约 688MB
```

在虚拟机内执行：

```bash
ollama pull openbmb/minicpm5:latest
ollama list
```

注意：

```text
688MB 是模型包 / 权重文件大小，不等于运行时完整 RAM 占用。
Ollama 加载后需要考虑 runtime、KV cache、推理 buffer、page cache 和请求波动。
```

采集内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-minicpm5-pull.txt
```

### 15.4 加载模型并观察内存

先启动一个极短请求：

```bash
ollama run openbmb/minicpm5:latest "只输出 OK"
```

另一个 SSH 终端采集：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-minicpm5-load.txt
```

观察重点：

```text
ollama / runner 进程 RSS。
MemAvailable 是否低于 350MB。
是否出现明显 swap。
是否出现 OOM kill。
首次请求是否极慢。
```

判定：

```text
MemAvailable >= 350MB：2GB + Ollama 路线有继续验证空间。
MemAvailable 250MB-350MB：可以跑通，但空间紧，必须写入报告。
MemAvailable < 250MB：不建议宣称 2GB 可稳定运行 Ollama + MiniCPM5。
发生 OOM：记录为 2GB/Ollama 边界，停止，不硬扛。
```

### 15.5 使用真实 Ollama 跑少量 structured output

只跑少量用例，不跑长 benchmark。

当前 CLI 的真实 Ollama 路线由 `eval --ollama` 触发，`parse` / `dry-run` 子命令当前只暴露 `--mock` 开关。因此这一阶段不要写不存在的 `dry-run --ollama` 命令。

执行：

```bash
cd /home/edge/edgehome

./edgehome --profile low_memory --config-dir configs --db-path artifacts/qemu-ollama-eval.sqlite \
  eval cases/zh-home.yaml --ollama \
  | tee artifacts/ollama-eval-low-memory.json
```

注意：

```text
当前 CLI 中 eval 支持 --ollama。
parse / dry-run 当前不作为真实 Ollama 验证入口。
不要为了单条 demo 命令强行改 CLI；如果未来需要，再单独规划 CLI 能力。
```

采集内存：

```bash
/home/edge/edgehome/scripts/collect-memory.sh \
  | tee /home/edge/edgehome/artifacts/after-minicpm5-structured-call.txt
```

验收：

```text
如果 structured output 成功，记录输出 JSON、耗时、内存水位。
如果超时，记录 timeout 和 MemAvailable。
如果 OOM，记录 dmesg / journalctl 中的 OOM 证据。
```

失败处理：

```text
Ollama 阶段失败不代表 EdgeHome Harness 失败。
它只说明 QEMU + 2GB + Ollama + MiniCPM5 的边界。
必跑主线仍以 Mock Harness eval gate 为准。
```

## 16. 阶段 12：生成嵌入式验证报告

最终报告建议写入：

```text
docs/qemu-embedded-validation-report.md
```

报告素材保存在：

```text
/mnt/e/edgehome-qemu/artifacts/
/home/edge/edgehome/artifacts/
```

报告必须包含：

```text
1. 验证日期。
2. Git commit hash。
3. 宿主机环境：Windows + WSL2 Ubuntu。
4. QEMU 启动命令。
5. Ubuntu cloud image 版本。
6. VM CPU / RAM 参数。
7. uname -m 输出。
8. free -h baseline。
9. EdgeHome ARM64 binary file 输出。
10. low_memory config show 输出摘要。
11. mock parse / dry-run 样例输出摘要。
12. eval --gate 输出摘要。
13. pressure 1024 / 400 / 128 输出摘要。
14. 内存快照对比表。
15. 可选 Ollama / MiniCPM5 阶段结果。
16. 明确限制：QEMU 不是树莓派真实性能 benchmark。
17. 后续真实开发板验证计划。
```

内存表建议格式：

```markdown
| 阶段 | MemTotal | MemAvailable | SwapFree | 最大 RSS 进程 | 结论 |
|---|---:|---:|---:|---|---|
| baseline | | | | | |
| edgehome config show | | | | | |
| eval --gate | | | | | |
| pressure policy | | | | | |
| ollama install | | | | | 可选 |
| minicpm5 loaded | | | | | 可选 |
```

结论必须分层：

```text
已证明：
  ARM64 Linux 2GB 预验证环境可运行 EdgeHome Harness mock low_memory 主链路。
  eval gate、trace、pressure policy 可在低内存环境验证。

未证明：
  真实树莓派长期稳定性。
  真实 token/s。
  真实米家设备控制。
  2GB + Ollama + MiniCPM5 长期稳定运行。

下一步：
  购买 4GB 树莓派用于主演示。
  购买或借用 2GB 设备用于 low_memory 极限验证。
```

## 17. 验收标准总表

### 17.1 必须通过

```text
QEMU virt ARM64 启动成功。
SSH localhost:2222 可登录。
uname -m = aarch64。
MemTotal 接近 2GB。
EdgeHome ARM64 release 二进制可运行。
./edgehome --help 可输出。
low_memory profile 可加载。
mock parse 可输出合法 JSON。
mock dry-run 可生成 trace / policy / dry-run 结果。
eval cases/zh-home.yaml 可运行。
eval --gate passed = true。
config pressure 1024 / 400 / 128 都可输出合法 JSON。
关键阶段内存快照已保存。
docs/qemu-embedded-validation-report.md 已生成。
```

### 17.2 可选通过

```text
Ollama ARM64 可安装。
openbmb/minicpm5:latest 可拉取。
模型加载后 MemAvailable >= 250MB。
至少一条 structured output 请求能完成。
Ollama 阶段有内存快照和结论。
```

### 17.3 失败即停止

```text
QEMU 无法启动。
MemTotal 不是 2GB 级别。
SSH 无法进入虚拟机。
ARM64 二进制无法运行。
mock eval --gate 不通过。
pressure policy 没有随内存水位变化。
报告无法复现验证过程。
```

### 17.4 失败但不阻塞主线

```text
Ollama 安装失败。
MiniCPM5 拉取失败。
MiniCPM5 加载 OOM。
QEMU 内 token/s 极慢。
真实 structured output 超时。
```

这些只影响“可选模型边界验证”，不推翻 Harness 主线。

## 18. 后续可沉淀脚本

本次先写计划。

后续 `/goal` 执行时可以逐步新增：

```text
scripts/qemu/setup-seed.sh
scripts/qemu/run-virt-2gb.sh
scripts/qemu/collect-memory.sh
scripts/qemu/copy-edgehome.sh
scripts/qemu/run-low-memory-eval.sh
scripts/qemu/README.md
```

脚本要求：

```text
默认路径使用 /mnt/e/edgehome-qemu。
启动脚本必须显式写 -m 2048。
脚本不得把模型、镜像、cargo target 放回 C 盘。
脚本必须能重复执行。
脚本输出必须适合写入 docs/qemu-embedded-validation-report.md。
```

## 19. Git 提交纪律

项目已有新规：

```text
经常提交留档。
```

嵌入式验证执行期间建议提交点：

```text
docs: add embedded validation plan
chore: add qemu setup scripts
docs: record qemu boot baseline
chore: add arm64 build notes
docs: add qemu low memory validation report
docs: record optional ollama minicpm5 boundary
```

每次提交前必须执行：

```bash
git status --short
```

提交原则：

```text
一次提交只包含一个阶段的可解释成果。
不要把用户未确认的博客、README 或临时数据库混进嵌入式验证提交。
不要提交 qcow2、seed.img、模型文件、cargo target、sqlite 临时库。
```

如需提交报告，允许提交：

```text
docs/qemu-embedded-validation-report.md
scripts/qemu/*.sh
scripts/qemu/README.md
```

禁止提交：

```text
*.qcow2
seed.img
AAVMF_CODE.fd
AAVMF_VARS.fd
target/
cargo-target/
*.sqlite
模型权重
```

## 20. 面试表达口径

完成本文档后，对外可以这样描述：

```text
我没有直接声称 1B 模型能在 2GB 树莓派上稳定商用，
而是先搭了一个 QEMU virt ARM64 2GB 的预验证环境。

这个环境用于验证 ARM64 Linux 部署、2GB 内存水位、low_memory profile、
Rust Harness 的 mock eval gate、内存压力降级、trace/replay/report 闭环。

模型部分我单独作为边界验证：Ollama + MiniCPM5 在 QEMU 里能否加载、
加载后 MemAvailable 是否还有 250MB-350MB 安全余量、
structured output 是否能跑少量用例。

因此项目不是 prompt demo，而是围绕端侧小模型的工程 Harness：
模型只负责候选 JSON，Rust 负责业务真相、记忆、策略、执行和观测。
```

如果被问“为什么不用 QEMU raspi4b”：

```text
raspi4b 更像树莓派形态，但在工程自动化、Ubuntu cloud image、virtio 磁盘、
SSH、CI 化验证上更脆弱。我的目标不是模拟树莓派每个外设，
而是验证 ARM64 Linux + 2GB RAM + EdgeHome Harness low_memory 主链路。
所以第一阶段用 qemu virt，真实性能和外设后续再上真实板卡。
```

如果被问“2GB 到底能不能跑”：

```text
2GB 可以作为 low_memory profile 极限验证目标，
但不是宽裕环境。MiniCPM5 q4 模型包约 688MB，
Ollama 加载后还会有 runtime、KV cache、推理 buffer、page cache 和请求波动。
所以 2GB 下必须无桌面、短上下文、短输出、单并发、低重试、trace buffer 受限，
并保留 MemAvailable 250MB-350MB。实际演示更推荐 4GB。
```

## 21. `/goal` 执行提示词

后续进入追求目标模式时，可以直接给出以下目标：

```text
请严格按照《嵌入式验证计划.md》执行 EdgeHome Harness 的 QEMU virt ARM64 2GB 嵌入式预验证。
不要扩大范围，不要做真实米家适配，不要自研推理引擎。
按阶段推进：先宿主机预检查，再 E 盘目录，再 QEMU 工具链，再 Ubuntu ARM64 cloud image，
再 cloud-init seed，再启动 2GB VM，再部署 ARM64 edgehome 二进制，
再跑 mock low_memory eval gate 和 pressure policy，最后生成 docs/qemu-embedded-validation-report.md。
每完成一个可验证阶段就提交留档。
Ollama / MiniCPM5 只作为可选边界验证，失败不推翻 Harness 主线。
```

执行完成的定义：

```text
本文档 17.1 的必须通过项全部满足。
docs/qemu-embedded-validation-report.md 已写入。
相关脚本和报告已提交。
最终回答明确哪些已证明、哪些未证明、下一步真实开发板怎么验证。
```
